# گزارش بررسی UI/UX و برندینگ — Parsa-apps.github.io

**نقش بررسی‌کننده:** طراح ارشد UI/UX و متخصص برندینگ وب
**روش بررسی:** مرور کامل کد (HTML/CSS/JS)، بررسی اسکرین‌شات‌های واقعی موبایل، تحلیل نسخهٔ منتشرشدهٔ سایت، ممیزی عملکرد و انیمیشن‌ها
**تاریخ:** مرداد ۱۴۰۵ (آگوست ۲۰۲۶)
**نتیجهٔ کلی:** نمرهٔ فعلی **۶.۵ از ۱۰** — صفحهٔ اصلی در سطح خوب و نزدیک به استاندارد استارتاپی است، اما ناهماهنگی هویت برند و صفحات داخلیِ ناتمام، اعتماد کاربر را می‌شکند.

---

## ۱) اولین حس کاربر در ۵ ثانیهٔ اول

**چه چیزی خوب کار می‌کند:**
- صفحه با یک هیروی تمیز و روشن باز می‌شود؛ تایپوگرافی فارسی درشت «پارسا اپس» با زیرنویس لاتین PARSA APPS بلافاصله خوانده می‌شود.
- سلسله‌مراتب پیام درست است: برچسب کوچک «استودیوی خلاق محصول دیجیتال» → نام برند → شعار «سازنده اپلیکیشن‌های خلاقانه و هوشمند» → توضیح یک‌خطی → دو CTA واضح.
- دکمهٔ اصلی «مشاهده محصولات» تیره و تمام‌عرض در موبایل است؛ مسیر بعدی کاربر روشن است.

**چه چیزی حس را خراب می‌کند:**
- **تضاد هویتی در همان ثانیهٔ اول:** لوگوی طلاییِ کنده‌کاری‌شدهٔ «PA» روی زمینهٔ مشکی، حسِ برند جواهرات، هتل لوکس یا موسسهٔ حقوقی می‌دهد — نه استودیوی اپلیکیشن‌های کودک و سرگرمی. کاربر در ۵ ثانیهٔ اول دو پیام متناقض می‌گیرد: «لوکس و کلاسیک» از لوگو، «شاد و نارنجی و کودکانه» از بقیهٔ صفحه.
- شیمر طلایی روی متن عنوان + جرقه‌ها + حلقهٔ چرخان دور لوگو، همه هم‌زمان در هیرو فعال‌اند؛ کمی «تلاش زیاد برای جلب توجه» حس می‌شود.
- تا قبل از لود فونت وزیرمتن (که از Google Fonts با `@import` لود می‌شود)، متن با Tahoma/Segoe رندر می‌شود و یک پرش بصری (FOUT) اتفاق می‌افتد.

**جمع‌بندی ۵ ثانیه:** «سایت مرتبی است، ولی این شرکت دقیقاً چیست؟ لوگو می‌گوید لاکچری، محصول می‌گوید بچه‌ها.» — این ابهام، مهم‌ترین مشکل ثانیه‌های اول است.

---

## ۲) کیفیت طراحی بصری

**نقاط قوت:**
- صفحهٔ اصلی از نظر ساختار، واقعاً حرفه‌ای طراحی شده: شماره‌گذاری سکشن‌ها (۰۱ / محصول اصلی، ۰۲ / ارزش‌ها)، کارت تیرهٔ «STUDIO 01» با گرید و نقاط تزئینی، فاصله‌گذاری منظم، شعاع گوشه‌های یکدست (`--radius: 22px`).
- کارت محصول «جزیره فندقی» با تصویر باکیفیتِ جزیرهٔ فندقی و ماسکوت، جذاب‌ترین بخش سایت است. تصویرسازی‌ها (خانه درختی، فندق بامزه با فانوس) عالی و باکیفیت‌اند.
- سکشن «مسیر آینده» با ویژوال مداری تیره، از نظر گرافیکی متمایز است.

**نقاط ضعف جدی:**
- **دو سایت در یک سایت:** صفحهٔ اصلی مدرن و پرداخت‌شده است، اما `about.html` و `privacy.html` عملاً **صفحات خام بدون هدر، فوتر و طراحی** هستند (فقط یک `h1` و یک پاراگراف). کاربری که از منو روی «درباره ما» می‌زند، از یک سایت حرفه‌ای به یک صفحهٔ سفید خالی پرت می‌شود — این بزرگ‌ترین شکاف کیفی سایت است و اعتماد را فوراً از بین می‌برد.
- **ایموجی به‌جای آیکون:** 🎨 🚀 🔒 💡 در کارت‌های ارزش‌ها و 🏝️ داخل عنوان محصول. ایموجی در هر سیستم‌عامل شکل متفاوتی دارد، با پالت برند هماهنگ نیست و حس «قالب آماده» می‌دهد. در اسکرین‌شات موبایل، آیکون‌های ایموجی (قفل طلایی، موشک) با سبک بقیهٔ صفحه ناسازگارند.
- در `island.html` استایل‌های inline (`style="font-size:1.15rem..."`) استفاده شده که نشانهٔ خروج از سیستم طراحی است.
- کارت‌های ارزش‌ها در موبایل بیش از حد بلند و «خالی» هستند: شمارهٔ کم‌رنگ بالا-چپ، ایموجی راست، متن پایین — فضای مرده زیاد است.
- صفحهٔ ۴۰۴ نیز مینیمال و بدون هدر/فوتر است (متن آن بامزه و خوب است: «این مسیر روی جزیره نیست»).

---

## ۳) هماهنگی لوگو، رنگ‌ها و فونت فارسی

**لوگو — مشکل شمارهٔ یک برند:**
- مونوگرام طلاییِ سه‌بعدی با تزئینات اسلیمی/باروک روی مشکی، زبان بصری «لوکس کلاسیک» دارد.
- پالت سایت (نارنجی `#f58a24`، فیروزه‌ای `#25b7a0`، کرم، سرمه‌ای `#0b1220`) زبان «استارتاپ شاد و مدرن» دارد.
- محصول اصلی (جزیره فندقی با ماسکوت فندق) زبان «کودکانه و فانتزی» دارد.
- نتیجه: **سه زبان بصری متفاوت در یک برند.** لوگوی طلایی در فوتر تیره خوب می‌نشیند، اما کنار کارت نارنجی-فیروزه‌ای هیرو و آیکون فندقی، وصله‌ای جدا به نظر می‌رسد. رندر سه‌بعدی/فتورئال لوگو هم آن را از دنیای Flat/مدرن سایت جدا می‌کند.

**رنگ‌ها:**
- خود پالت (سرمه‌ای + نارنجی + فیروزه‌ای + کرم) انتخاب خوبی است و به‌صورت توکن‌های CSS تمیز تعریف شده. تضاد رنگی متن اصلی مناسب است.
- ولی طلایی لوگو (`#b77c16`–`#f0cf75` در شیمر) رنگ پنجمی است که فقط به‌خاطر لوگو وارد سیستم شده و با نارنجی برند رقابت می‌کند.

**فونت فارسی:**
- **وزیرمتن انتخاب کاملاً درستی است** — خوانا، مدرن و استاندارد دیفاکتوی وب فارسی. وزن‌های متنوع (۴۰۰ تا ۸۰۰) هوشمندانه استفاده شده و `line-height: 1.85` برای متن فارسی عالی است.
- سه ایراد فنی/تایپوگرافیک:
  1. لود با `@import` در ابتدای CSS = زنجیرهٔ رندربلاک (CSS → Google Fonts CSS → فایل فونت). برای کاربر ایرانی با تأخیر بالا، پرش فونت محسوس است. فونت باید self-host و `preload` شود.
  2. `letter-spacing` منفی (تا `-0.075em`) روی عنوان فارسی: فاصله‌گذاری حروف در خط فارسی/عربی به‌دلیل اتصال حروف ریسک بصری دارد و در برخی مرورگرها اتصال حروف را خراب نشان می‌دهد. برای فارسی بهتر است صفر باشد.
  3. در بخشی از CSS فونت `Arial` هاردکد شده (خط ۱۰۱۰) که ناسازگار با سیستم است.

---

## ۴) تجربهٔ کاربری موبایل

**نقاط قوت (بر اساس اسکرین‌شات‌های واقعی اندروید):**
- هیرو در موبایل خوب چیده شده؛ دکمه‌ها تمام‌عرض با ارتفاع لمسی مناسب.
- منوی همبرگری با `aria-expanded`، بستن با Escape و کلیک بیرون — پیاده‌سازی درست.
- لینک پرش به محتوا (skip-link)، `aria-label`ها و `aria-current` — سطح دسترس‌پذیری بالاتر از میانگین سایت‌های فارسی.
- تشخیص دستگاه ضعیف/صرفه‌جویی داده (`saveData`, `hardwareConcurrency`) و خاموش‌کردن افکت‌های تزئینی — کار مهندسی تحسین‌برانگیزی است.

**نقاط ضعف:**
- **صفحهٔ اصلی در موبایل خیلی طولانی است:** کارت تیرهٔ «STUDIO 01» تقریباً یک صفحهٔ کامل را می‌گیرد و عملاً تکرار لوگویی است که کاربر همین الان در هیرو دیده. سکشن «مسیر آینده» هم نزدیک دو صفحهٔ اسکرول فقط ویژوال مداری تزئینی است. کاربر موبایل قبل از رسیدن به فوتر، ۴-۵ صفحه محتوای تزئینی اسکرول می‌کند در حالی که فقط «یک» محصول واقعی معرفی می‌شود.
- کارت‌های ارزش‌ها در موبایل هرکدام تقریباً نصف صفحه‌اند و ۴ کارت پشت‌سرهم با فضای خالی زیاد، ریتم را کند می‌کند.
- در صفحهٔ جزیره فندقی، دکمهٔ «دانلود به‌زودی» غیرفعال + اسکرین‌شات‌های placeholder («به‌زودی») = بن‌بست برای کاربر موبایل که با هدف دانلود آمده. «خبرم کنید» هم فقط به صفحهٔ تماس می‌رود، نه فرم عضویت واقعی.
- ترنزیشن مصنوعی ۲۳۰ میلی‌ثانیه‌ای بین صفحات (fade قبل از ناوبری) روی موبایل، سایت را «کندتر از واقعیت» حس می‌دهد.

---

## ۵) سرعت و عملکرد انیمیشن‌ها

**کیفیت مهندسی انیمیشن: بالاتر از حد انتظار.** کد `script.js` واقعاً حرفه‌ای نوشته شده:
- `IntersectionObserver` با `unobserve` بعد از اجرا، `requestAnimationFrame` با throttle، فقط `transform/opacity` (کامپوزیت GPU)، پاک‌کردن `will-change` بعد از settle.
- احترام کامل به `prefers-reduced-motion` حتی با گوش‌دادن به تغییر زندهٔ آن؛ خاموشی افکت‌ها هنگام مخفی‌شدن تب؛ خاموشی لایهٔ ambient برای سکشن‌های خارج از دید.
- تیلت سه‌بعدی محدود به ±۲.۲ درجه و فقط دسکتاپ/ماوس — سنجیده.

**اما مشکل، «مقدار» است نه «کیفیت»:**
- بیش از **۴۰ keyframe** فعال: شیمر عنوان، چرخش حلقه، جرقه‌ها، مدارها، ستاره‌ها، ابرها، حباب‌ها، ذرات، blob، ریپل، شاین لوگوی هدر، bob اسکرول‌کیو... حتی وقتی همه ۶۰fps اجرا شوند، نتیجهٔ ادراکی «شلوغی» است. سایت‌های استارتاپی سطح بالا (Linear، Stripe، Vercel) با ۳-۴ حرکتِ کم اما دقیق، حس گران‌بودن می‌سازند؛ اینجا ۱۵+ حرکت هم‌زمان حس «تِم آماده» می‌دهد.
- انیمیشن‌های بی‌نهایت (`infinite`) روی موبایل حتی با بهینه‌سازی، مصرف باتری دارند.
- **بودجهٔ لود:** فونت با `@import` رندربلاک است؛ `parsa-apps-gold-360.png` حدود ۹۲KB است (به‌صورت WebP می‌تواند ~۲۵KB باشد)؛ وزن تصاویر صفحهٔ اصلی ~۴۴۰KB قابل‌قبول ولی قابل‌بهبود است. ضمناً فایل‌های مرده و سنگین در مخزن وجود دارد (`assets/hero.png` = ۴.۱MB بدون هیچ ارجاعی، آیکون‌های ۱۰۲۴px چند مگابایتی، پوشهٔ `uploadforchatgbt`) که پروژه را سنگین و نامرتب می‌کند.

---

## ۶) آیا سایت حس یک شرکت حرفه‌ای ساخت اپلیکیشن را منتقل می‌کند؟

**پاسخ صادقانه: نیمه‌حرفه‌ای — «استودیوی تک‌نفرهٔ بااستعداد»، نه «شرکت».**

آنچه حس حرفه‌ای می‌دهد:
- صفحهٔ اصلی مدرن، متادیتای کامل (OG، Schema.org، manifest، sitemap، robots)، دسترس‌پذیری خوب، متن فارسی روان و بدون غلط، تصویرسازی محصول باکیفیت.

آنچه حس حرفه‌ای را می‌شکند (به ترتیب شدت):
1. **صفحات «درباره ما» و «حریم خصوصی» خام و بدون طراحی** — اولین جایی که کاربرِ جدی (والدین، ناشر، سرمایه‌گذار) سر می‌زند.
2. **ایمیل Gmail شخصی** (`farshadparsa2019@gmail.com`) به‌عنوان کانال رسمی + دامنهٔ `github.io` = سیگنال «پروژهٔ شخصی».
3. تنها محصول سایت **قابل دانلود نیست** و اسکرین‌شات واقعی ندارد («به‌زودی» × ۲).
4. صفحهٔ «کارتونیا» ساخته شده و در sitemap هست ولی **از هیچ‌جای سایت لینک نشده** (صفحهٔ یتیم) — ناهماهنگی محتوایی.
5. هیچ نشانه‌ای از اعتماد اجتماعی نیست: نه آمار دانلود، نه نظر کاربر، نه لینک استور (کافه‌بازار/مایکت)، نه تیم.
6. متن حریم خصوصی دوخطی — برای اپ کودک، که حساس‌ترین حوزهٔ اعتماد والدین است، به‌شدت ناکافی است.

---

## ۷) خلاصهٔ نقاط ضعف طراحی

| # | ضعف | شدت |
|---|------|------|
| ۱ | ناهماهنگی هویت: لوگوی طلایی لوکس × پالت استارتاپی × محصول کودکانه | 🔴 بحرانی |
| ۲ | صفحات about / privacy / 404 بدون هدر، فوتر و سیستم طراحی | 🔴 بحرانی |
| ۳ | ایموجی به‌جای آیکون سفارشی در کل سایت | 🟠 بالا |
| ۴ | بن‌بست دانلود + اسکرین‌شات placeholder در صفحهٔ محصول | 🟠 بالا |
| ۵ | تراکم بیش از حد انیمیشن (۴۰+ keyframe) → حس تم آماده | 🟠 بالا |
| ۶ | فونت با @import (رندربلاک + FOUT) و letter-spacing منفی روی فارسی | 🟠 بالا |
| ۷ | Gmail شخصی + دامنهٔ github.io | 🟠 بالا |
| ۸ | صفحهٔ اصلی موبایل بیش‌ازحد طولانی با سکشن‌های صرفاً تزئینی | 🟡 متوسط |
| ۹ | صفحهٔ یتیم کارتونیا؛ ناهماهنگی منو/فوتر بین صفحات | 🟡 متوسط |
| ۱۰ | فایل‌های مردهٔ سنگین در مخزن (hero.png ۴.۱MB و…) | 🟡 متوسط |
| ۱۱ | فرم تماس فقط mailto (روی خیلی از دستگاه‌ها کار نمی‌کند) | 🟡 متوسط |
| ۱۲ | استایل‌های inline در island.html؛ Arial هاردکد در CSS | 🟢 کم |

---

## ۸) پیشنهادهای دقیق برای رسیدن به سطح استارتاپ‌های حرفه‌ای

### فاز ۱ — رفع اعتمادشکن‌ها (بیشترین اثر، کمترین هزینه)
1. **صفحات about، privacy و 404 را با همان هدر/فوتر/سیستم طراحی صفحهٔ اصلی بازسازی کنید.** «درباره ما» باید داستان برند، مأموریت و چهرهٔ سازنده را داشته باشد؛ «حریم خصوصی» برای اپ کودک باید کامل باشد (چه داده‌ای جمع نمی‌شود، COPPA-مانند، تبلیغات، ارتباط والدین).
2. **تکلیف دانلود را روشن کنید:** تا آماده‌شدن نسخهٔ عمومی، سکشن دانلود را به «فرم خبرم کن» واقعی (مثلاً Formspree رایگان) تبدیل کنید و شمارندهٔ «در حال توسعه — انتشار پاییز ۱۴۰۵» بگذارید. دکمهٔ غیرفعال هرگز.
3. **اسکرین‌شات‌های واقعی اپ** را در قاب موکاپ گوشی قرار دهید. اگر اپ آماده نیست، حتی ۳ نمای واقعی از نسخهٔ فعلی بهتر از placeholder است.
4. صفحهٔ کارتونیا را یا به منو/سکشن محصولات اضافه کنید یا از sitemap حذف کنید.
5. فرم تماس را به سرویس فرم (Formspree/Web3Forms) وصل کنید؛ mailto فقط به‌عنوان جایگزین.

### فاز ۲ — یکپارچه‌سازی برند
6. **تصمیم برند بگیرید:** یا لوگو را به یک مونوگرام Flat و هندسی (نسخهٔ ساده‌شدهٔ PA با رنگ نارنجی/سرمه‌ای برند) بازطراحی کنید، یا کل زبان سایت را به سمت «پریمیوم تیره + طلایی» ببرید. پیشنهاد حرفه‌ای: نسخهٔ وکتور تخت از همان PA (فایل‌های SVG موجود در assets/brand پایهٔ خوبی‌اند)، طلایی فقط به‌عنوان رنگ تأکید (accent) در حالت تیره.
7. **یک ست آیکون واحد** (مثلاً Lucide یا آیکون‌های سفارشی خطی با رنگ برند) جایگزین همهٔ ایموجی‌ها شود — این تغییر به‌تنهایی ظاهر سایت را یک پله بالا می‌برد.
8. **دامنهٔ اختصاصی + ایمیل سازمانی** (مثل `parsaapps.ir` یا `parsaapps.com` و `hello@…`) — مهم‌ترین سیگنال «شرکت واقعی».
9. راهنمای برند (brand.html موجود است — خوب!) را با قواعد استفاده از طلایی/نارنجی و فاصله‌گذاری لوگو کامل کنید.

### فاز ۳ — پرداخت تجربه و عملکرد
10. **فونت وزیرمتن را self-host کنید** (woff2، فقط وزن‌های ۴۰۰/۶۰۰/۸۰۰) با `<link rel="preload">` و `font-display: swap`؛ `@import` حذف شود. `letter-spacing` منفی روی عناوین فارسی صفر شود.
11. **رژیم انیمیشن:** قانون «حداکثر یک انیمیشن محیطی در هر viewport». شیمر عنوان یا حلقهٔ چرخان — یکی، نه هر دو. ذرات/ستاره/ابر/حباب در موبایل کاملاً حذف شوند (کد شما از قبل زیرساختش را دارد). ترنزیشن ۲۳۰ms بین صفحات حذف یا به ۱۲۰ms کاهش یابد.
12. **تصاویر:** تبدیل PNGهای لوگو به WebP/AVIF با `<picture>`، افزودن `srcset` به تصویر هیروی محصول، حذف فایل‌های بدون ارجاع (hero.png ۴.۱MB، آیکون‌های ۱۰۲۴ استفاده‌نشده، پوشهٔ uploadforchatgbt).
13. صفحهٔ اصلی موبایل را کوتاه‌تر کنید: کارت «STUDIO 01» در موبایل مخفی یا خیلی فشرده شود؛ ویژوال مداری «مسیر آینده» در موبایل به یک ردیف چیپ ساده تقلیل یابد؛ کارت‌های ارزش‌ها به گرید ۲×۲ فشرده.
14. **اعتماد اجتماعی اضافه کنید:** بج «به‌زودی در کافه‌بازار / مایکت»، سکشن کوچک «چرا والدین اعتماد می‌کنند» (بدون تبلیغ، بدون ردیابی، آفلاین)، و بعد از انتشار: آمار دانلود و نظرات.
15. متادیتا و og:image اختصاصی برای صفحات داخلی (island، contact) اضافه شود؛ sitemap با `lastmod` به‌روز شود.

---

## جمع‌بندی نمره‌ای

| معیار | نمره از ۱۰ |
|---|---|
| اولین برداشت (۵ ثانیه) | ۶.۵ |
| کیفیت بصری صفحهٔ اصلی | ۸ |
| کیفیت بصری صفحات داخلی | ۳ |
| هماهنگی برند (لوگو/رنگ/فونت) | ۵ |
| تجربهٔ موبایل | ۷ |
| مهندسی انیمیشن | ۹ |
| اعتدال و سلیقهٔ انیمیشن | ۶ |
| عملکرد و سرعت | ۶.۵ |
| حس «شرکت حرفه‌ای» | ۵.۵ |
| **میانگین کلی** | **۶.۵** |

**سه اقدامی که بیشترین جهش را می‌سازند:** (۱) بازسازی صفحات داخلی با سیستم طراحی اصلی، (۲) حل تضاد لوگوی طلایی با هویت شاد برند، (۳) جایگزینی ایموجی‌ها با آیکون‌های برند. با همین سه مورد، سایت از «پروژهٔ شخصی خوش‌ساخت» به «استودیوی قابل‌اعتماد» می‌رسد.
